doctype html
html(lang='en')

	include ./components/head/mixin-head.pug
	+head({
		title: 'Продается всё!',
		description: [ 'Уезжаю из Петербурга, избавляюсь от накопившегося имущества.' ],
		baseUrl: '.'
	})

	body

		include ./components/header/mixin-header.pug
		+header()

		include ./components/category/mixin-category.pug
		+category(stuff.audio)
		+category(stuff.books)
		+category(stuff.health)

		script(src='./sale.js')
